import {connectNative} from './native-cdp.mjs';const c=await connectNative();console.log(await c.evaluate('JSON.stringify({status:document.querySelector(".native-view-status")?.textContent,alerts:[...document.querySelectorAll("[role=alert]")].map(e=>e.textContent),timings:window.dndromWorldTimings})'));console.log(await c.evaluate('window.ue.dndrom.dispatch(JSON.stringify({method:"app.diagnostics",params:{}})).then(JSON.parse)'));c.close();
